const state = {
  token: "",
  username: "",
  id: "",
};

const getters = {
  getToken: (state) => state.token,
  getUser: (state) => state.username,
  getId: (state) => state.id,
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
