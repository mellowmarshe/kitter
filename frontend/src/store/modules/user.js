const state = {
  token: "",
  username: "",
};

const getters = {
  getToken: (state) => state.token,
  getUser: (state) => state.username,
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
